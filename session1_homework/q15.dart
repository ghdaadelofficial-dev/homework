void main (){
  /*A statically typed language requires you to 
  specify the type of every variable
   before using it.
Types can be int, String, double, bool.
The type is checked at compile time, 
not at runtime.*/




/* 
How it impacts the way variables are used:
You must declare the type of a variable
 before using it (or let Dart infer it with var).
You cannot change the type of a variable once 
it is assigned.
Example: if you declare int age = 20;,
 you cannot later assign age = "20";.*/
}