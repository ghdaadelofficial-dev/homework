void main(){
  /* In a Dart app,
   the main function has a very important purpose
   it is the entry point of the program.
    In other words,
     every Dart program starts executing from 
     the main() function.
     ##any cone inside main execute first
     ##if the programme doesn't have main 
     dart won't know where is start ,
     and progrmme won't run */
}